## RabbitMQ server settings (POC)
## Guest remote access: apps connect from other Docker containers (not 127.0.0.1).
loopback_users.guest = false

## Do not load exported definitions here — a stale definitions.json can overwrite
## RABBITMQ_DEFAULT_USER / RABBITMQ_DEFAULT_PASS with the wrong password hash.
